<h1>Orders</h1>
<ul>
  <% @orders.each do |order| %>
    <li><%= link_to "Order ##{order.id}", order_path(order) %> — User: <%= order.user.name %>, Total: $<%= order.total %></li>
  <% end %>
</ul>
